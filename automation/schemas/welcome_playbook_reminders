{
  "title" : "Welcome Playbook Reminder",
  "type" : "array",
  "minItems": 0,
  "items" : {
    "anyOf": [
      {
        "type": "object",
        "properties": {
          "minutes_after": {"type": "number"},
          "title": {"type": "string"},
          "message": {"type": "string"},
          "url": {"type": "string"},
          "icon": {"type": "string"},
          "image": {"type": "string"},
          "enabled": {"type":  "boolean"}
        },
        "required": ["title", "message", "minutes_after", "url", "enabled"]
      }
    ]
  }
}